# In local development, requests to /api are proxied to the backend automatically
# (see vite.config.js) so you usually don't need this file locally.
#
# In production, set this to your deployed backend's URL, e.g.:
# VITE_API_URL=https://your-backend.onrender.com
VITE_API_URL=
